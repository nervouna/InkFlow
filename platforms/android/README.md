# Android project foundation

This settings project deliberately contains no application module yet. The
version catalog records the native toolchain and Android plugin declarations
that the real IME module will consume.

The Gradle wrapper, application module, manifest, JNI bridge, and Kotlin source
arrive together with the Android implementation so this foundation cannot be
mistaken for a buildable input method.
