# InkFlow

自用的 macOS 中文输入法项目，采用 Rime（librime）作为二次开发基础。

当前仅包含目录骨架，尚未接入引擎、实现输入功能或配置构建。

```text
InkFlow/
├── macOS/     # macOS 输入法源码
└── schemas/   # Rime 输入方案与词典
```

目录中的 `.gitkeep` 仅用于让 Git 保留空目录。
